## 1. Базовый слой ошибок со стеком

- [x] 1.1 Создать пакет `internal/stackerr` с моделью кадров стека и типом ошибки, реализующим `error`, `Unwrap`, `Is`, `As` и `slog.LogValuer`
- [x] 1.2 Реализовать публичные конструкторы `New`, `Wrap` и `WithStack` с семантикой одноразового захвата стека
- [x] 1.3 Реализовать захват/фильтрацию стека через `runtime.Callers` с лимитом глубины кадров и исключением служебных кадров `runtime`/`internal/stackerr`
- [x] 1.4 Добавить unit-тесты на форму сериализации (`msg`, `cause`, `stack_trace`) и совместимость с `errors.Is`/`errors.As`
- [x] 1.5 Добавить unit-тесты, подтверждающие отсутствие дублирования стека при повторных цепочках wrap/with-stack

## 2. Миграция рантайм hotspot-путей

- [x] 2.1 Перевести создание/оборачивание ошибок в `internal/app/runtime_dependencies.go` с `%w`-обёрток на `stackerr.Wrap` там, где это уместно
- [x] 2.2 Перевести распространение ошибок в `internal/worker/execution_service.go`, чтобы возвращаемые/логируемые рантайм-ошибки несли стек через `Wrap`/`WithStack`
- [x] 2.3 Перевести распространение ошибок в `internal/worker/claim_loop.go`, чтобы сырые пути `return err` получали стек на граничных точках
- [x] 2.4 Сохранить существующие log call sites там, где уже передаётся `"error", err`, и подтвердить автоматическую структурную сериализацию через `LogValuer`

## 3. Интеграция с lifecycle observability

- [x] 3.1 Расширить lifecycle-хелперы атрибутов в `internal/observability/events.go` (или добавить sibling-хелперы), чтобы принимать опциональные объекты `error` вместе с `error_code`/`diagnostic_message`
- [x] 3.2 Обновить lifecycle log call sites в `internal/worker/claim_loop.go`, чтобы включать структурированный `error`, сохраняя текущие lifecycle-поля
- [x] 3.3 Обновить lifecycle log call sites в `internal/worker/execution_service.go` и `internal/browser/session_restorer.go` с тем же подходом совместимости
- [x] 3.4 Добавить тесты, подтверждающие, что lifecycle-логи по-прежнему содержат `error_code` и `diagnostic_message`, а также теперь включают структурированный `error` со стеком

## 4. Валидация, совместимость и безопасность rollout

- [x] 4.1 Добавить/обновить интеграционные тесты (или golden-log тесты), подтверждающие, что `error` эмитится как объект для ошибок со стеком
- [x] 4.2 Запустить наборы тестов репозитория, покрывающие bootstrap приложения, рантайм воркера и пути observability-логирования
- [x] 4.3 Задокументировать переход формата логов (`error` строка → объект) и ожидаемое влияние на потребителей в заметках к change
- [x] 4.4 Проверить, что не добавлены внешние зависимости, и обновить developer docs/примеры, где показан старый формат поля `error`
